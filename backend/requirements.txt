fastapi>=0.109.0
uvicorn[standard]>=0.27.0
sqlalchemy>=2.0.0
aiosqlite>=0.19.0
python-multipart>=0.0.6
pillow>=10.2.0
pystray>=0.19.5
aiofiles>=23.2.1
pydantic>=2.5.0
pydantic-settings>=2.1.0
httpx>=0.27.0
yt-dlp>=2026.7.4

# AI auto-tagging is OPTIONAL and intentionally NOT part of the base install or
# the shipped binary. It pulls in a large ML stack (torch/CUDA, onnxruntime,
# transformers). Install it on demand into your NekoBooru Python environment:
#   GPU (NVIDIA/CUDA):  pip install -r backend/requirements-tagger.txt
#   CPU only:           pip install -r backend/requirements-tagger-cpu.txt
# then enable it in the web UI (Settings > Auto Tagging).
